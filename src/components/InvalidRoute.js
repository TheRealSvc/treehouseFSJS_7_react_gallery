import React from 'react';

const InvalidRoute = () => (
  <div className="not-found">
    <h3> Ups, this Route does not exist </h3>
    <p> Please use the predefined buttons or use the searchForm </p>
</div>
);

export default InvalidRoute;